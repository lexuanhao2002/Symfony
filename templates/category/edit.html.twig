{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    <div class="form-group">
    <label for="category">Category name</label>
    {{ form_widget(form.CatName,{
        'attr':{
            'class':'form-control'
        }
    }) }}
    <input type="submit" name="edit_category" value="Edit" class="btn btn-primary"/>
    {{ form_end(form) }}
{% endblock %}